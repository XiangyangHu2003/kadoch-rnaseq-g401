---
title: "Project Progress Report For 01 and 02"
author: "Xiangyang Hu"
format: html
---

# Reproducible bulk RNA-seq analysis of SMARCB1/BAF47 re-expression in G401 malignant rhabdoid tumor cells

## 1. Background

SMARCB1/BAF47 is a core component of the SWI/SNF (BAF) chromatin remodeling complex, and its disruption is strongly associated with tumor development in multiple cancer types. In this project, I used public bulk RNA-seq data to analyze transcriptional changes following SMARCB1/BAF47 re-expression in G401 malignant rhabdoid tumor cells. The overall goal is to identify genes and biological programs altered after BAF47 restoration.

## 2. Objective

The main objective of this project is to compare **Empty** and **BAF47 re-expression** samples and address the following questions:

1. Which genes show significant expression changes after SMARCB1/BAF47 re-expression?
2. Does BAF47 re-expression reshape the overall transcriptional program of tumor cells?
3. Can a standard and reproducible RNA-seq workflow be built from public data to support downstream statistical analysis and biological interpretation?

## 3. Dataset and Samples

This analysis currently uses four G401 Day 3 RNA-seq samples:

- G401_Empty_Day3_A
- G401_Empty_Day3_B
- G401_BAF47_Day3_A
- G401_BAF47_Day3_B

Each raw sample file contains gene-level read count information. The four sample-level files were merged by `GeneSymbol` to create a unified count matrix for downstream DESeq2 analysis.

## 4. Data Preprocessing

### 4.1 Count matrix construction

The raw sample files did not contain column headers, so column names were assigned manually after import. The two key columns extracted from each file were:

- `GeneSymbol`
- `Read.Count`

For each sample, the `Read.Count` column was renamed using the corresponding sample ID, and all four samples were merged by `GeneSymbol` to generate a single `count_matrix.csv` file.

### 4.2 Metadata preparation

A sample metadata table (`metadata.csv`) was created with the following fields:

- `condition`
- `timepoint`
- `replicate`

The metadata rows were reordered to match the column order of the count matrix exactly, ensuring that sample annotations were correctly aligned with expression counts.

### 4.3 DESeq2 object construction

The count matrix, metadata table, and the design formula `~ condition` were combined into a DESeq2 dataset object using `DESeqDataSetFromMatrix()`. Before downstream analysis, low-count genes were filtered out, retaining only genes with a total count greater than 10 across all samples.

## 5. Quality Control

### 5.1 Library size assessment

Total read counts were summarized for each sample and visualized in a library size bar plot. This step was used to examine whether sequencing depth was broadly comparable across samples and to identify any obvious outlier sample with unusually low or high total counts.

Current status:

- All four samples were successfully included in the analysis workflow.
- A library size summary table was exported to `results/tables/library_size.csv`.
- The corresponding bar plot was saved to `results/figures/library_size_barplot.png`.
- Initial observation: **[Add a brief interpretation here: for example, whether the samples are in a similar range or whether one sample appears lower than the others.]**

### 5.2 PCA analysis

Before PCA, count data were transformed using variance stabilizing transformation (`vst`) to reduce heteroscedasticity and the dominance of highly expressed genes. PCA was then performed on the transformed data to evaluate global sample structure.

The purpose of PCA was to assess:

- whether replicate samples cluster together,
- whether Empty and BAF47 samples show separation at the transcriptome level.

Current status:

- The PCA plot was successfully generated.
- PC1 explains **99%** of the variance.
- PC2 explains **1%** of the variance.
- Initial observation: **[Add a brief interpretation here: for example, whether the two conditions appear separated and whether replicates are relatively close to each other.]**

### 5.3 Sample-to-sample correlation heatmap

A Pearson correlation matrix was computed from the VST-transformed expression matrix, and a sample-to-sample correlation heatmap was generated. This analysis was used as an additional QC step to assess overall similarity among samples and to identify potential outliers.

Current status:

- The correlation matrix was saved to `results/tables/sample_correlation_matrix.csv`.
- The heatmap was saved to `results/figures/sample_correlation_heatmap.png`.
- Initial observation: **[Add a brief interpretation here: for example, whether within-condition samples show stronger similarity.]**

## 6. Differential Expression Analysis

After preprocessing and QC, differential expression analysis was performed using DESeq2 with the following contrast:

- `BAF47` vs `Empty`

Two result tables were generated:

- `results/tables/deseq2_results_all.csv`: all tested genes
- `results/tables/deseq2_results_sig.csv`: significantly differentially expressed genes

Significant genes were defined using the following thresholds:

- adjusted p-value (`padj`) < 0.05
- absolute log2 fold change > 1

This means that selected genes are not only statistically significant but also show at least an approximately two-fold expression change.

Current status:

- DESeq2 analysis was completed successfully.
- The number of significant genes is **2314**.
- The MA plot was saved to `results/figures/ma_plot.png`.

## 7. Work Completed So Far

At the current stage, the following components have been completed:

1. Raw sample-level read count import and merging
2. Count matrix construction
3. Metadata preparation and sample order validation
4. DESeq2 dataset construction
5. Low-count gene filtering
6. Library size QC
7. PCA analysis
8. Sample-to-sample correlation heatmap
9. DESeq2 differential expression analysis
10. Export of figures and result tables

## 8. Current Significance of the Project

From a technical perspective, this project has already established a reproducible bulk RNA-seq workflow including:

- data import and organization,
- metadata management,
- count-based differential expression analysis,
- QC visualization,
- export of analysis outputs.

From a biological perspective, this project provides a structured framework for examining how SMARCB1/BAF47 re-expression influences transcriptional programs in malignant rhabdoid tumor cells.

## 9. Next Steps

The next stage of the project will include:

1. Visualization of differential expression results
   - volcano plot
   - heatmap of top differentially expressed genes

2. Functional enrichment analysis
   - GO Biological Process
   - Hallmark gene sets
   - GSEA

3. Interpretation of pathway-level changes associated with BAF47 re-expression

4. Refinement of the project into a polished GitHub presentation and job application portfolio item

## 10. Reproducibility Statement

This project was implemented in R. The main packages used so far include:

- tidyverse
- DESeq2
- pheatmap

Intermediate tables, figures, and result files were saved to organized output directories to support inspection, reproducibility, and future extension of the workflow.
